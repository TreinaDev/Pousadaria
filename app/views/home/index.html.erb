<h1>Pousadaria</h1>

<section id="inns">
  <div class="row">
  <% @inns.each do |inn| %>
    <div class="col-4">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= link_to inn.name, inn %></h5>
        </div>
      </div>
    </div>
  <% end %>
  </div>
</section>